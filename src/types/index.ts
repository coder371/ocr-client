export interface GoogleOcr {
  sur_name: string;
  given_name: string;
  birth_date: string;
  issue_date: string;
  expiry_date: string;
  gender: string;
  nationality: string;
}

export interface Image {
  uri: string;
}